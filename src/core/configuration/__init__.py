"""Configuration helpers (validation, save, backups) for JSON config files.

This package contains utilities to validate configs against JSON Schemas,
perform atomic writes with backups, and publish change events.
"""

__all__: list[str] = [
    # Re-export common service types when needed in the future
]
